// JavaScript Document
function openmenu(c_Str){
	if(document.all(c_Str).className==""){
		document.all(c_Str).className="opensubmenu";
		}
	else{
		document.all(c_Str).className="";
		}
	};
